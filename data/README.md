# Data Directory

## Raw Data
Place raw, unprocessed data files in the `raw/` subdirectory.

## Processed Data
Place cleaned and processed data files in the `processed/` subdirectory.

## Important Notes
- Do not commit large data files directly to the repository
- Use `.gitignore` to exclude large data files
- Document data sources in the project documentation
- Consider using data version control tools for large datasets
